"""Auth request and response schemas."""

# TODO(Tim): Request- und Response-Schemas fuer Login definieren.
# Ziel: LoginRequest aus routes/auth.py hierher verschieben und eine klare LoginResponse
# mit status, message, username und role definieren.
# Fertig, wenn auth.py keine eigenen Pydantic-Klassen mehr enthaelt.
