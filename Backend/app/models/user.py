"""Planned split-out model for users.

Keep Backend/app/models.py as the active model module until the migration is done.
"""

# TODO(Tim): User-Model aus Backend/app/models.py hierher verschieben.
# Ziel: User als eigenes Datenbankmodell pflegen, ohne SecurityEvent und Alert in derselben Datei.
# Fertig, wenn alle Imports auf app.models.user zeigen und die App weiter startet.
